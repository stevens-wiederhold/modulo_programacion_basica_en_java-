$ git add . 
agregar archivos 

$ git status 
estarus de archivos

$ git commit -M "Creacion de directorios"
guardar todos los cambios en el repositorio local

$ git config --gloval user.name "stevens"
$ git config --gloval user.email  

DESCARGAR https://git-scm.com/download/win
